# env files (all levels)
.env
.env.*
.env.local
.env.production
*.env

# Firebase / Google Services
mobile/google-services.json
mobile/GoogleService-Info.plist
mobile/ios/**/GoogleService-Info.plist

# dependencies
node_modules/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.claude/worktrees/
.next/
supabase/.temp/

# EAS (root-level artifacts created by eas-cli, real configs live in mobile/)
/app.json
/eas.json

# tmp artifacts
tmp/

# Vercel CLI link metadata (created by `vercel link`, contains project + org IDs)
.vercel

# container mount artifacts (e.g. WSL / dev-container writes that captured
# absolute-path writes during local development)
mnt/

# HTML prototypes dropped at repo root — keep design work out of the tree
/dashboard-*.html

# MCP config (local per-developer)
.mcp.json
.vscode/mcp.json

# Playwright MCP logs
.playwright-mcp/

# Audit screenshots
audit/

# Planning docs & prompts (personal, not code)
*.docx
/prompts-*.md
/prompt-*.md
/kinevo-*.md

# Design work + specs locais (orchestrator workflow)
/SPEC_FASE_*.md
/Kinevo_*.html
/Kinevo*.html
/Kinevo *.html
/Kinevo_*.md
/Diagnostico_*.md
/wearables-mockup.html
/_kinevo-uxui-figures/
/EXERCISE_ITEM_CARD_BEFORE_REFACTOR.txt
docs/prd/PROMPT_claude_code_preferencias.md
.claude/commands/

# Expo / iOS build artifacts on root (em caso de invocação errada
# do `expo run:ios` da raiz em vez de `cd mobile`)
/.expo/
/ios/

# Orchestrator scratchpad — gitignored
/_planning/specs/
/_planning/mocks/
# Note: /_planning/analysis/ é versionado (docs estratégicos)

# macOS lixo (.DS_Store da raiz já listado acima; cobertura recursiva)
**/.DS_Store

# Word/Pages lockfiles
~$*
