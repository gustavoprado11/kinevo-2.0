# QA Visual Loop — 2026-06-15

## Resumo

- **Telas analisadas (9):** dashboard, students, programs, financial, marketing/landing, schedule, forms, exercises, settings — todas HTTP 200, sem erros de console.
- **Regressões:** 0 reais · 0 by_design · 0 false_positive (nenhuma regressão reportada em nenhuma tela).
- **Melhorias:** 0 que valem (`absent`/`partial`) · 4 descartadas (1 `already_exists` + 3 `low_value`).
- **Ação necessária:** nenhuma. Estado saudável.

## ✅ Regressões confirmadas (verdict=real)

Nenhuma regressão confirmada nas 9 telas.

## 💡 Melhorias que valem (status=absent ou partial)

Nenhuma melhoria acionável — todas as sugestões foram descartadas na verificação (ver tabela abaixo).

## 🛠️ Prompts de fix prontos

Nenhum. Não há regressões `real` nem melhorias `absent`/`partial` de impacto `high`/`med`.

## 🗑️ Descartados na verificação

| Item | Veredito | Motivo (file:line) |
|---|---|---|
| Diferenciar o card 'Carteira Kinevo' como opção principal (financial) | low_value | Onboarding tem 3 cards informativos e 1 único CTA 'Começar' que avança o passo; destaque visual é estético, não bloqueia o fluxo. `web/src/components/financial/financial-onboarding.tsx:62-92` (cards) + `:94-100` (CTA) |
| Definir a URL pública direto no editor de conteúdo (marketing/landing) | low_value | Empty-state já instrui 'Defina sua URL pública no card acima'; o controle vive em `landing-section.tsx:262-273` via estado interno `editing`, sem prop/handler exposto — refator de baixo retorno. `landing-editor.tsx:891-901` |
| Mostrar contagem de resultados filtrados perto dos filtros (exercises) | already_exists | Bloco 'Results Count' em `web/src/components/exercises/exercises-client.tsx:517-521` já renderiza `{filteredExercises.length} exercícios` e anexa ` de {total}` quando há filtro ativo (ex.: '42 exercícios de 568'); reativo a busca/grupo/padrão/'Meus exercícios' (`:252-269`). Total fixo separado no header (`:403`). |

> Os 3 itens acima vinham marcados como sugestões mas foram filtrados contra o código — nenhum vira ação. As demais 6 telas não geraram nem regressão nem sugestão (resultado limpo, não invenção).