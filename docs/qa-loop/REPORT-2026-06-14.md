# QA Visual Loop — 2026-06-14

## Resumo
- Telas analisadas: 9 (`/dashboard`, `/students`, `/programs`, `/financial`, `/marketing/landing`, `/schedule`, `/forms`, `/exercises`, `/settings`) — todas HTTP 200, sem erros de console.
- Regressões: 0 reais / 0 by_design / 0 false_positive (nenhuma regressão reportada).
- Melhorias: 2 que valem (status `absent`) vs 2 descartadas (`already_exists`, `low_value`).
- Ação efetiva: 1 prompt de fix (impacto `med`, rota `/schedule`). A melhoria de `/programs` é impacto `low` → só listada.

## ✅ Regressões confirmadas (verdict=real)
Nenhuma. Todas as 9 telas passaram sem regressões — bom resultado.

## 💡 Melhorias que valem (status=absent ou partial)
| Tela | Impacto | O quê | Porquê |
|---|---|---|---|
| `/schedule` | med | Sinalizar que clicar num horário cria agendamento | Slot vazio só tem `cursor-pointer transition-colors`; sem affordance `+`/hover/CTA, o usuário não descobre que `handleBackgroundClick` cria o agendamento (weekly-calendar.tsx:172-189; subtítulo inerte em schedule-client.tsx:251). |
| `/programs` | low | Desabilitar/ocultar a barra de busca no empty-state | Input de busca renderiza incondicionalmente acima do empty-state, ficando inerte enquanto não há programas (programs-client.tsx:423-432 vs 449-462). Cosmético; só foco visual. |

## 🛠️ Prompts de fix prontos
```
Na rota /schedule (apps/web .../weekly-calendar.tsx), adicione uma affordance visual de "clique para criar" nos slots de horário vazios da grade semanal. Hoje a coluna em weekly-calendar.tsx:188 só tem `cursor-pointer transition-colors` e o único realce de hover é o estado de drag `isOver` (linha 189), então o usuário não descobre que handleBackgroundClick (172-179) cria um agendamento. Adicione um realce de hover no slot vazio (ex.: leve background + um ícone "+" centralizado que aparece no group-hover) reutilizando os tokens de cor já usados na grade. Não altere o comportamento de drag/drop existente. Resultado: ao passar o mouse sobre um horário livre em /schedule, fica claro que clicar ali cria um novo agendamento.
```
Outcome: torna descobrível a criação de agendamento por clique na grade de `/schedule`.

## 🗑️ Descartados na verificação
| Item | Veredito | Motivo |
|---|---|---|
| `/programs` — desabilitar busca no empty-state | low_value | Campo é inerte, não engana nem quebra nada; CTA "Criar primeiro programa" já domina (programs-client.tsx:423-432 / 449-462). Ganho de uso nulo. |
| `/settings` — dar label ao traço solto abaixo de "Kinevo Pro" | already_exists | Já renderiza `formatCurrency(effectiveAmount)`/`getIntervalLabel` quando há preço (billing-section.tsx:168-184); o "—" só aparece com `effectiveAmount===null` — empty-state proposital (billing-section.tsx:186, consistente com formatDate em :44). |

---
_Nota: `/programs` aparece duas vezes (uma como melhoria `low`, outra descartada como `low_value`) porque dois agentes avaliaram a mesma sugestão da barra de busca com vereditos distintos — sinal de que é borderline; tratar como não-ação._
