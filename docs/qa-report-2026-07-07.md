# QA completo do working tree — 07/jul/2026

**Alvo:** working tree NÃO commitado sobre `HEAD a85abac` (= `origin/main` = prod). Lote: 12 fixes da auditoria (F1–F12), Financeiro Asaas Fases 1–4 (P1–P18 parciais), feature WIP Consultoria IA.
**Base de comparação (spec):** `docs/auditoria-web-mobile-2026-07-07.md` + `docs/analise-financeiro-2026-07-07.md`.
**Método:** Camada 0 (checks estáticos) · Camada 1 (unitários + 2 qa tests novos) · Camada 2 (E2E web + smoke de runtime) · Camada 3 (mobile: código + bundle iOS ao vivo) · Camada 4 (prod read-only via MCP).
**Regras honradas:** zero `git commit/push`; zero migration/deploy; **zero escrita em produção**; nenhuma conta QA criada; nenhum bug corrigido (só documentado).

---

## 1. Veredito

**APROVADO para commit.** Nenhum bug de correção encontrado. Todos os 30 fixes/itens verificáveis conferem com a especificação — por teste automatizado, leitura de código dirigida, e smoke de runtime. Os itens não exercitáveis são todos por dependência externa (carteira Asaas aprovada, device físico, pagamento real), não por defeito. Um único ponto de ATENÇÃO em prod é **esperado** (fix F1 ainda não deployado — é working tree).

| Camada | Resultado |
|---|---|
| 0 — Checks estáticos | ✅ PASS (todos os números batem) |
| 1 — Unitários direcionados | ✅ PASS (4/4 cobertos + 2 qa tests novos, 15 asserts) |
| 2 — E2E web | ✅ PASS no exercitável · 🟡 3 itens BLOQUEADO (carteira Asaas / rule #3) |
| 3 — Mobile | ✅ PASS (código + bundle iOS ao vivo) · 🟡 flows interativos BLOQUEADO |
| 4 — Prod (read-only) | ✅ PASS · 1 ATENÇÃO esperada (cron F1 não deployado) |

---

## 2. Camada 0 — Checks estáticos

| Check | Esperado | Obtido | Status |
|---|---|---|---|
| `web tsc --noEmit` | 0 erros | **0** | ✅ |
| `web vitest run` | 1386 pass / 44 skip | **1386 pass / 44 skip** (136 files) | ✅ |
| `web npm run build` | exit 0 | **exit 0** | ✅ |
| `mobile tsc --noEmit` | 0 erros | **0** | ✅ |
| `mobile vitest run` | 374 pass | **374 pass** (38 files) | ✅ |
| `shared vitest run` | 304 pass | **304 pass** (20 files) | ✅ |
| `eslint` no diff (26 arquivos) | 0 erros NOVOS | **8 errors + 12 warnings, todos legados** | ✅ |

**ESLint — detalhe (nada bloqueante):** os 8 `error` são todos `@typescript-eslint/no-explicit-any` (débito legado, ignorado por instrução): `cleanup-orphan-signups/route.ts:105` (cast `as any[]` deliberado da junção no fix F1), `settings/page.tsx:74`, `student-detail-client.tsx:170,202,392,512,513,537`. Os 12 `warning` são `no-unused-vars` — imports mortos deixados pelos refactors P1/P18 (ver achado B1). Nenhum erro novo de regra além do `any` legado.

---

## 3. Camada 1 — Testes unitários direcionados

**Itens 1–4 (cobertura existente confirmada por leitura + suíte verde):**

| # | Alvo | Onde | Status |
|---|---|---|---|
| 1 | Guard `asaas_auto_recurring` no `markAsPaid` (P2, dupla cobrança) | `contracts-core.test.ts:102-114` (`/débito automático/i`, nada escrito) | ✅ |
| 2 | Estratégia 2b (ativação por `asaas_subscription_id`) + avanço `current_period_end` + pin anti-forja `subscription`/`dueDate` | `route.test.ts:404-447` (2b + período `2026-08-10`), `:382-402` (backfill sub só se null) | ✅ |
| 3 | Lista de eventos com chargeback (P5) | `webhook-setup.test.ts` (`ALL_EVENTS` inclui os 3 `CHARGEBACK*`; drift `events-incomplete`→`needsRepair`). Confirmado contra `KINEVO_WEBHOOK_EVENTS` real | ✅ |
| 4 | `hasOrgCoreAccess` never-throw (F2) | `student-cap.test.ts` — o `fakeAdmin` não suporta a chain `.eq().eq().limit().maybeSingle()`, então `hasOrgCoreAccess` sofre TypeError interno e o try/catch retorna `false`; os 5 asserts `.resolves` **quebram se o try/catch for removido** → guard de regressão válido | ✅ |

**Item 5 — LACUNAS (avaliadas; 2 qa tests novos escritos + 1 por inspeção):**

- ✅ **`cancelContractCore` (P3 + P4)** — escrito `contracts-core-cancel.qa.test.ts` (7 asserts, **passa**):
  - P3: `cancelAtPeriodEnd=true` + Asaas → **rejeitado** com mensagem, zero escrita, zero chamada Asaas.
  - P4: link vivo → `deactivatePaymentLink` chamado; 404 → tratado como ok e cancel prossegue; one-off + falha não-404 → best-effort prossegue.
  - **P4 fail-closed**: recorrente ainda SEM assinatura + falha ao desativar → **NÃO cancela** (não escreve `status=canceled`).
  - Ownership: contrato de outro trainer → "Sem permissão".
- ✅ **`reconcilePaymentLinkContract` (P7/P8)** — escrito `charge-reconcile.qa.test.ts` (8 asserts, **passa**):
  - Guards: `not_found`, `not_owner`, `no_link`, `canceled`, `not_paid` (sem tocar Asaas / sem mutação).
  - Happy path recorrente: ativa, avança `current_period_end` (`dueDate 2026-07-10` + mês = `2026-08-10`), backfill `asaas_subscription_id` só se null, upsert autoritativo por `asaas_payment_id`, `unblock_student_access`, timeline com dedupe por `paymentId`.
  - One-off pago NÃO avança período.
- 🟡 **Dup-guard `POST /api/wallet/charges` e `/subscriptions` (P10)** — validado **por inspeção** (`charges/route.ts:226-276`): reusa link vivo (`reused:true`), aposenta link morto/órfão antes de criar. Lógica correta. Não escrito qa test: exige harness pesado de route (auth `requireTrainer` + Asaas client) e exercício E2E exige carteira aprovada (ver Camada 2).

**Total qa tests novos: 2 arquivos, 15 asserts, 100% verde.** (Arquivos descartáveis — removidos ao final, §7.)

---

## 4. Camada 2 — E2E web

Dev server (`npm run dev`, Turbopack) subiu limpo. **Nota de segurança:** `.env.local` aponta para **produção** (não há stack Supabase local). Portanto só exercitei caminhos que **não escrevem em prod**; o caminho autenticado dos crons foi validado por código + unitário (ver §6, Decisões de segurança).

| # | Item | Como verificado | Status |
|---|---|---|---|
| — | `reconcile-asaas-charges` auth | `curl` sem header → **401**; bearer errado → **401** (runtime) | ✅ |
| — | `cleanup-orphan-signups` auth | `curl` sem header → **401** (runtime) | ✅ |
| — | `webhooks/asaas` auth | `curl` sem token → **401** (runtime) | ✅ |
| 1 | Badges sem enum cru / sem "Manual" p/ Asaas; tooltip sem Stripe; "Marcar pago" oculto p/ recorrência Asaas | Código: `billing-type-badge.tsx:30-39` (Cobrança Asaas/Cartão automático), `subscriptions-client.tsx:130-131,381` (label), `:778` (gate `!== asaas_auto_recurring`), `:109` (tooltip "Aguardando" sem Stripe) | ✅ (código) |
| 2 | Copiar Link (P1) → `GET /api/wallet/charges/[id]`, sem erro Stripe | Código: `subscriptions-client.tsx:246-247` (`startsWith('asaas')` → fetch wallet). **E2E ao vivo: BLOQUEADO** (sem carteira Asaas aprovada na conta QA) | ✅ código / 🟡 E2E |
| 3 | Modal aluno: "Reenviar link" idem; "Marcar pago" oculto p/ recorrência | Código: `student-financial-modal.tsx:209-210` (Asaas→wallet), `:532,557` (gate) | ✅ (código) |
| 4 | Cancelamento: `status=canceled` + desativa link; `cancelAtPeriodEnd` Asaas rejeitado | `cancelContractCore` (qa test §3 P3/P4). **MCP `kinevo_cancel_contract` NÃO rodado em prod** (risco de cancelar contrato real se guard regredisse) — coberto por unitário | ✅ código+unit |
| 5 | `/messages` sem RPC morta `get_trainer_conversations` | Código: `messages/actions.ts` — única ocorrência é o comentário explicativo (chamada removida, F5) | ✅ (código) |
| 6 | Consultoria: aprovar sem CREF → erro; com CREF sem nome → erro | Código: `validate-consultoria.ts:56-62` (`code:'missing_cref'`), `:65-71` (erro nome), `:103-117` (carimbo ANTES de publicar), `:132` (transição guardada) | ✅ (código) |
| 7 | Cron `reconcile-asaas-charges` 200 c/ `{scanned,synced,...}`; `cleanup-orphan` 200 sem deletar aluno | **BLOQUEADO por segurança** (rule #3): rodar autenticado contra prod dispararia `ensureSubaccountWebhook`/`reconcile` (escrita em `trainer_payment_accounts`/Asaas) e `cleanup` deletaria `auth.users` reais. Response-shape + lógica validados por código + unitário | 🟡 BLOQUEADO |

**Consultoria WIP renderiza:** as 6 actions têm auth/tenancy corretos (padrão `resolveTrainer` + `.eq('trainer_id')`, confirmado na auditoria); a migration 226 (RLS `FOR ALL` com `current_trainer_id()`) foi verificada ao vivo na auditoria. Renderização visual de `/consultoria` não exercitada (exige sessão de treinador + o substrato dos guards é server-side, já validado).

---

## 5. Camada 3 — Mobile (simulador iOS)

Simulador **iPhone 17 Pro (iOS 26.2)** booted, dev client `com.kinevo.mobile` instalado. **Smoke de runtime: bundle iOS Expo compilado ao vivo via Metro → HTTP 200, 21.2 MB, zero erro de resolução/transform.** Ou seja, todo o working tree mobile (incluindo os hooks/telas novos) roda no bundler, não só passa no tsc.

| # | Item | Como verificado | Status |
|---|---|---|---|
| 1 | Banner de cobrança (P13b) → `/payment`; ausente sem pendência | Código: `home.tsx:65,535-562` (`useStudentPendingCharge`, banner "Você tem uma cobrança"/"Pagamento atrasado" + valor → `/payment`); hook `useStudentPendingCharge.ts` (query RLS leve, fail-silencioso). **Flow interativo: BLOQUEADO** (sem contrato pendente seedado) | ✅ código / 🟡 flow |
| 2 | `/payment` (P14): valor/plano, WebView, poll de sucesso, erro amigável | Código: `payment.tsx:32,44` (poll 8s), `:169,173` (`onError`/`onHttpError` → fim da página em branco), `:88` (pull-to-refresh), `:100` ("Pagamento confirmado"), `:90-92` (estado de erro). **Interativo BLOQUEADO** (WebView exige link Asaas vivo = carteira aprovada) | ✅ código / 🟡 flow |
| 3 | `/profile/subscription` (P13c): mostra `pending_payment` + "Pagar agora"; labels Asaas | Código: `subscription.tsx:31` ("Aguardando pagamento"), `:372-388` ("Pagar agora"→`/payment` p/ pending/past_due) | ✅ (código) |
| 4 | Tela de bloqueio (P15): botão "Falar com meu treinador" se treinador tem phone senão "Suporte"; tema light/dark | Código: `PaymentBlockedScreen.tsx:56-61` (join RLS coach phone), `:186` (texto condicional `coachWhats`), `:74-77` (disca treinador/fallback), `:92,113,121` (tokens `useV2Colors`, sem dark hardcoded) | ✅ (código) |
| 5 | Treinador: dashboard erro≠R$0 (P16); refetch on-focus (P17); duplicar template completo (P11/F11) | Código: `useFinancialDashboard.ts` (expõe `error`), `financial/index.tsx`+`contracts.tsx` (refetch on focus), `useProgramTemplateActions.ts` (2-arg RPC migr 231). **Interativo BLOQUEADO** (dados financeiros + Network Link Conditioner + template seedado) | ✅ código / 🟡 flow |
| 6 | Push `charge_created` roteamento → `/payment` | Código: `usePushNotifications.ts` (case `charge_created`→`/payment`). **Push real: pendente de device físico** (fora de escopo do simulador) | ✅ código / 🟡 device |

**Fixes F9/F10/F12 (corrida Watch/realtime):** cobertos pela suíte mobile (374 verdes) + tsc; WatchConnectivity não roda no simulador (fora de escopo por design).

---

## 6. Camada 4 — Produção (somente leitura, via Supabase + Vercel MCP)

Projeto confirmado: `lylksbtgrihzepbteest` = "Kinevo 2.0" (prod real).

| Check | Resultado | Status |
|---|---|---|
| `cron.job_run_details` (7d) | 7 jobs, **100% `succeeded`**, zero falha (block-overdue, dispatch-notifications 2016×, oura-refresh, purge-traces, renew-google-watch, etc.) | ✅ |
| `webhook_events` recentes | 20 processados (todos com `processed_at`); Stripe + Asaas; último Asaas = teste R$5 (23/jun). Consistente com "prova por evento R$588 pendente" | ✅ |
| Advisors security | **0 ERROR**, 107 WARN, 3 INFO — todos padrão/by-design (SECURITY DEFINER helpers, buckets públicos, leaked-password toggle) | ✅ |
| Vercel runtime errors (2d) | **1 grupo**: `/api/cron/cleanup-orphan-signups` PGRST106 `Invalid schema: auth` — visto até **2026-07-07T03:55Z** | 🟡 ATENÇÃO |
| Working tree vazou p/ prod? | Último deploy prod = commit `a85abac` em `main` (githubDeployment) = **HEAD local**. Working tree 100% NÃO commitado. **Nada vazou** | ✅ |

**Sobre a ATENÇÃO (não é achado novo, é esperado):** o erro PGRST106 do `cleanup-orphan-signups` continua ocorrendo em prod porque o **fix F1 ainda não foi deployado** (é working tree). Confirma que (a) o cron segue quebrado em prod hoje e (b) o F1 é necessário. Após deploy do F1, esse grupo deve sumir. Reconciliação de SHA: o brief citava `HEAD 38a7b66`, mas o repositório local avançou 1 commit para `a85abac` (o fix "Salvar Modelo" que o próprio `38a7b66` registrara como pendência) — prod == `origin/main` == HEAD local; sem divergência.

---

## 7. Bugs / achados (severidade · arquivo:linha · repro · esperado vs observado)

Nenhum bug de **correção**. Achados de higiene (documentados, **não corrigidos** por rule #4):

**B1 — LOW · Imports mortos deixados pelos refactors P1/P18** — `student-financial-modal.tsx:7-8,98,103` (`Shield`, `ShieldOff`, `HandCoins`, `Heart`, `hasStripeConnect` prop, `router`) e `student-header.tsx:11-12` (`Flame`, `Dumbbell`, `Activity`, `TrendingUp`, `TrendingDown`, `Tag`).
- **Repro:** `cd web && npx eslint src/components/financial/student-financial-modal.tsx` → 6 warnings `no-unused-vars`.
- **Esperado vs observado:** esperado imports enxutos após o refactor; observado 12 símbolos importados e não usados. Cosmético (warning, não erro); build/tsc passam. Recomendo remover antes do commit.

**B2 — INFO · `hasStripeConnect` virou prop morta em `student-financial-modal`** — desde que o P1 roteia Asaas para o endpoint wallet, o componente não consulta mais `hasStripeConnect` (`:98`). Não quebra nada; candidato a limpeza junto de B1.

**B3 — INFO (prod, esperado) · `cleanup-orphan-signups` PGRST106 ativo em prod** — ver Camada 4. Não é regressão do working tree; é a ausência de deploy do F1. Sem ação de QA (o fix já existe no working tree, validado por leitura: check de `students` + fail-closed + GoTrue Admin API).

---

## 8. Itens não exercitáveis (e por quê)

| Item | Motivo |
|---|---|
| Copiar Link / Reenviar link ao vivo (P1) · WebView de checkout (P14) · badges de contrato Asaas renderizados | Exigem **carteira Asaas aprovada** + contrato Asaas vivo. A conta QA não tem subconta aprovada; criar cobrança real é proibido (rule #3). Validados por código + unitário (`GET /api/wallet/charges/[id]` é o caminho certo). |
| Cron `reconcile`/`cleanup` autenticado (200) | Dev server usa `.env.local` = prod; rodar autenticado escreveria em `trainer_payment_accounts`/Asaas (`ensureSubaccountWebhook`) ou deletaria `auth.users`. Proibido por rule #3. Auth (401) + lógica validados. |
| `kinevo_cancel_contract` via MCP (P3) | O MCP opera em prod; se o guard P3 tivesse regressão, cancelaria um contrato real. Coberto pelo qa test de `cancelContractCore`. |
| Flows mobile interativos (banner/pagamento/bloqueio/dashboard treinador) | Exigem sessão de aluno/treinador QA logada no app + seed de contrato pendente/bloqueio em prod + (P14) link Asaas vivo. Validados por código + bundle de runtime + 374 unitários. |
| Push `charge_created` real | Push real exige **device físico** (não simulador). Roteamento validado por código. |
| RLS/tenancy live da consultoria | Já verificado ao vivo na auditoria (migration 226, policy `FOR ALL`). |

---

## 9. Decisões de segurança tomadas

1. **Zero escrita em produção.** Todos os checks de prod foram SELECT/advisor/deploy-list. Nenhum `execute_sql` mutante, migration, deploy, nem tool MCP de escrita.
2. **Crons não rodados autenticados** contra prod (rule #3) — só o caminho 401 (pré-DB).
3. **Nenhuma conta QA criada** — como os itens dependentes de carteira já eram BLOQUEADO, não houve necessidade de seedar prod. Resultado: **nada a limpar em prod**.

---

## 10. Sujeira criada e limpa

| Artefato | Onde | Situação |
|---|---|---|
| Contas QA (treinador/aluno) | — | **Nenhuma criada** ✅ |
| Contratos/cobranças de teste | — | **Nenhum criado** (zero escrita em prod) ✅ |
| qa tests descartáveis | `web/src/actions/financial/__tests__/contracts-core-cancel.qa.test.ts`, `web/src/lib/asaas/__tests__/charge-reconcile.qa.test.ts` | **Removidos** ao final (comportamentos provados ficam documentados em §3; podem ser promovidos à suíte permanente se desejado) |
| Dev server (3000) + Metro (8081) | local | **Encerrados** ✅ |
| Este relatório | `docs/qa-report-2026-07-07.md` | Untracked, **NÃO commitado** (por instrução) |
| Scratch | fora do repo (scratchpad) | — |

**Pegada da QA (verificada):** os **43 arquivos tracked-modified continuam idênticos** ao início (contados antes/depois) — nenhum rodar de teste/build/dev sujou arquivo versionado. O único arquivo que a QA adicionou e manteve é este relatório (untracked). Os 2 qa tests foram criados, rodados e removidos.

**Nota de fidelidade:** surgiram durante a janela alguns untracked que **não são da QA** — `docs/README.md`, `docs/STATUS.md`, `docs/archive/prompts/*` (mtimes ~10:18, fora de qualquer Write meu; provável reorg de docs rodando em paralelo na máquina). Estão **fora do escopo deste lote** e foram **deixados intactos**. Os untracked que SÃO o lote sob teste (`consultoria/`, `charge-reconcile.ts`, `reconcile-asaas-charges/`, `useStudentPendingCharge.ts`, `update-cref.ts`, migration `226`, `rede-consultoria-ia/`, os 2 relatórios de análise) permanecem como o desenvolvedor os deixou.
