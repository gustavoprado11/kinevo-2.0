{
  "name": "kinevo-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "mobile": "npm run start --workspace=mobile",
    "gen:types": "supabase gen types typescript --project-id $SUPABASE_PROJECT_REF > shared/types/database.ts"
  }
}