# `legacy/` — DEPRECATED

Paleta atual em produção (web + mobile). Existe **apenas para preservar o visual**
durante a Fase 0 (extração de tokens) sem regressão.

A partir da Fase 1, componentes serão migrados gradualmente para `v2/`. Quando
nenhum consumidor referenciar mais `legacy/`, esta pasta será removida.

Não adicione tokens novos aqui. Se precisar de um token novo, adicione em `../v2/`.
