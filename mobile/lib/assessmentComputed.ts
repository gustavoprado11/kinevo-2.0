// A lógica vive em shared/lib/assessment-computed.ts (FONTE ÚNICA). Antes este
// arquivo e web/src/lib/assessment-computed.ts eram cópias-espelho que driftaram
// (M10). Re-export para preservar os imports locais existentes.
export * from '@kinevo/shared/lib/assessment-computed';
