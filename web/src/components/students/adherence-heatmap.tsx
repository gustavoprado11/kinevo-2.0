// Deprecated — intentionally empty.
//
// The 4-week adherence heatmap was removed during Sprint 2 after user
// feedback that it duplicated information already covered by the top stats
// (Treinos totais / Esta semana / Último treino) plus the navigable calendar
// below. Keeping a heatmap on top of those was visual noise without a
// complementary question to answer.
//
// The consolidated AdherenceCard that briefly wrapped this component was
// also removed (see adherence-card.tsx).
//
// Safe to delete this file from the repo; kept as an empty module only
// because the current Cowork filesystem does not expose `rm` and leaving a
// commented-out implementation would mislead future readers.
export {}
