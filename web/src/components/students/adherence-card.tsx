// Deprecated — intentionally empty.
//
// This component was created during the Sprint 2 adherence consolidation to
// merge the weekly sparkline and the 4-week heatmap into a single block. It
// was removed because the existing top stats (Treinos totais / Esta semana /
// Último treino) plus the navigable calendar below already answer the
// "how is this student doing?" question without adding a heatmap on top.
//
// Safe to delete this file from the repo; kept as an empty module only
// because the current Cowork filesystem does not expose `rm` and leaving a
// commented-out implementation would mislead future readers.
export {}
