// Deprecated — intentionally empty.
//
// These tests covered the AdherenceHeatmap component, which was removed
// during Sprint 2 (see ../adherence-heatmap.tsx for the rationale). The
// file is kept only because the Cowork filesystem does not expose `rm`.
//
// Vitest requires at least one test in a test file, so we keep a single
// skipped placeholder to avoid "No test found" errors.
import { describe, it } from 'vitest'

describe.skip('AdherenceHeatmap (removed)', () => {
    it('is no longer part of the UI', () => {
        // intentionally empty
    })
})
