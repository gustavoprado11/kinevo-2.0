import { redirect } from 'next/navigation'

/** A agenda do estúdio vive na Agenda normal (/schedule, filtro por treinador). */
export default function EstudioAgendaPage() {
    redirect('/schedule')
}
